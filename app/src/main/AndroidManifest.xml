<?xml version="1.0" encoding="utf-8"?>
<!--
    Elendheim Spark manifest.

    Note what is NOT here: there is no INTERNET permission and no network
    access of any kind. The app never talks to a server. Your ideas live only
    on this device, and the only way anything leaves is a file you explicitly
    export. The manifest is the privacy policy.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".SparkApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElendheimSpark">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ElendheimSpark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
